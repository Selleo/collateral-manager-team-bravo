<div class="nested-fields">
	<%= f.input :strength %>
	<!-- %= f.input :tag_name % -->
	<!-- %= f.collection_select(:tag_kind_id, TagKind.all, :id, :kind,
							{include_blank: "Select type",
							label: false,
							input_html: {class: "form-control"}}) % -->

	<%= @selection = f.collection_select(:id, TagKind.all, :id, :kind,
							{include_blank: "Select type",
							label: false,
							input_html: {class: "form-control"}}) %>
	<%= f.collection_select(:id, Tag.all, :tag_kind_id, :name,
							{include_blank: "Select kind",
							label: false,
							input_html: {class: "form-control"}}) %>							
		<div>
			<%= link_to_remove_association 'Remove tag', f %>
		</div>
</div>